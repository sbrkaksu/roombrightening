app.async_mainloop()
    ↓
GUI çalışır + asyncio çalışır
    ↓
program başlangıcı:
    create_artnet_interface()
    read_monitor_continuously()  ← sonsuz monitor loop
    ↓
kullanıcı proband txt yükler
    Phase(txt) → dict
    ↓
kullanıcı phase seçer
    set_phase()
    set_sequence()
    tablo dolar
    ↓
kullanıcı Durchgang starten der
    run_sequence()
    ↓
asyncio.create_task(run_sequence_task())
    ↓
run_sequence_task()
    ↓
for each scene:
    set_scene()
    activate_scene()
    timer başlat
    kullanıcı reaksiyon verebilir
    monitor oku/kaydet
    fade_scene()
    ISI
    gerekirse Nein yaz
    save()
    sonraki scene
    ↓
sequence bitince:
    UI reset
    phase completion kontrolü
    pause timer



txt dosyası
    ↓ read()
str
    ↓ literal_eval()
dict
    ↓ deney sırasında modify edilir
dict
    ↓ printer.pformat()
str
    ↓ file.write()
txt dosyasına kaydedilir


Ana yapı:
- App: GUI, QLC+/ArtNet, serial monitor, deney akışı.
- Phase: proband txt dosyasını okuyup/yazıyor, completion kontrolü yapıyor.
- run_sequence_task(): sahneleri sırayla çalıştırıyor, pause/continue/reset, ISI, monitor ölçümü ve reaksiyon kaydı burada.
- set_scene(): Spot ve E değerine göre DMX/ArtNet kanal değerlerini ayarlıyor.
- generate_and_load_phase_fein(): grob sonuçlarından fein proband üretiyor.
- appflow.txt: akışı özetliyor; GUI başlar, ArtNet ve monitor başlar, proband yüklenir, phase seçilir, sequence çalışır, sonuçlar txt'ye kaydedilir.
